export class Rectangle {
    constructor(x, y, width, height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}

export class Vec2 {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}
